server {
    listen 80;

    # Angular frontend
    location / {
        proxy_pass http://frontend:80;
    }

    # Spring Boot API
    location /api/ {
        proxy_pass http://backend:8080/;
        rewrite ^/api(/.*)$ $1 break;
    }
}
