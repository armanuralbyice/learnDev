server {
    listen 80;

    # FRONTEND
    location / {
        proxy_pass http://frontend:80;
    }

    # BACKEND API
    location /api/ {
        proxy_pass http://backend:8080;
        rewrite ^/api(/.*)$ $1 break;
    }
}
